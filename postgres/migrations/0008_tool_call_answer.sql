-- Что инструмент ответил. Отказ брокера едет внутри ответа, а не ошибкой
-- протокола: 25 августа отклонённая заявка была записана как успешный вызов, и
-- по записи её нельзя было отличить от исполненной.

ALTER TABLE tool_calls ADD COLUMN IF NOT EXISTS answer TEXT;
