-- Таблица отказов не имела ни одного писателя: отказ приходит от шлюза, а шлюз
-- стоит отдельным сервисом и пути в эту базу не имеет. Пустой раздел на странице
-- читается как «агента ни разу не останавливали», а это не то же самое, что
-- «мы этого не знаем». Возвращаем вместе со шлюзом, который её заполняет.
--
-- То, во что упирается заявка сегодня, видно в tool_calls: неудачный вызов несёт
-- сообщение брокера.

DROP TABLE IF EXISTS refusals;
